//03  (a)................................
const isPrimeNumber =(num)=>{
     if(num %2===0 && num >0)return true;
     return false;
}
const number=-2;
 const result = isPrimeNumber(number);
 console.log(result);

 // if num is positive and num%2 is equal to zero then it return true otherwise false .